<!DOCTYPE html>
<html>
<head>
<style>
    :root {
        --bg: #0a0a0a;
        --border: #222;
        --text: #00ff41; /* Classic Terminal Green */
        --dim: #004411;
    }
    body { 
        background: var(--bg); color: var(--text); 
        font-family: 'Consolas', monospace; margin: 0; padding: 20px;
    }
    .dashboard {
        display: grid;
        grid-template-columns: 1fr 300px;
        grid-template-rows: 1fr 200px;
        gap: 10px;
        height: calc(100vh - 40px);
    }
    .panel {
        border: 1px solid var(--border);
        background: #000;
        padding: 15px;
        overflow: hidden;
        position: relative;
    }
    .panel-header {
        position: absolute; top: 0; left: 0; right: 0;
        background: var(--border); color: #888;
        font-size: 10px; padding: 2px 10px; text-transform: uppercase;
    }
    /* Data Stream Table */
    table { width: 100%; border-collapse: collapse; margin-top: 15px; font-size: 12px; }
    th { text-align: left; color: var(--dim); border-bottom: 1px solid var(--border); padding: 5px; }
    td { padding: 5px; border-bottom: 1px solid #111; }
    
    .status-bar { height: 100%; width: 0%; background: var(--text); transition: width 0.1s; }
    .gauge-bg { background: var(--dim); height: 10px; width: 100%; }

    #rating-box {
        display: flex; align-items: center; justify-content: center;
        flex-direction: column; font-size: 40px; font-weight: bold;
        color: #ff0000; /* Red for the -5 rating */
    }
</style>
</head>
<body>

<div class="dashboard">
    <div class="panel">
        <div class="panel-header">Process_Kernel_Stream</div>
        <table id="stream-table">
            <thead>
                <tr>
                    <th>TIMESTAMP</th><th>PROCESS_ID</th><th>LOAD</th><th>STATUS</th>
                </tr>
            </thead>
            <tbody id="stream-body">
                </tbody>
        </table>
    </div>

    <div class="panel" id="rating-box">
        <div class="panel-header">User_Sentiment_Metric</div>
        <div id="big-score">-5.0</div>
        <div style="font-size: 10px; color: #555;">CRITICAL_REJECTION_LEVEL</div>
    </div>

    <div class="panel" style="grid-column: span 2;">
        <div class="panel-header">Hardware_Resources</div>
        <div style="margin-top: 20px;">
            <p>CPU_ALLOCATION</p>
            <div class="gauge-bg"><div class="status-bar" id="cpu-bar"></div></div>
            <p>MEMORY_USAGE</p>
            <div class="gauge-bg"><div class="status-bar" id="mem-bar"></div></div>
        </div>
    </div>
</div>

<script>
    const body = document.getElementById('stream-body');
    const cpuBar = document.getElementById('cpu-bar');
    const memBar = document.getElementById('mem-bar');

    // Function to generate fake technical rows
    function addRow() {
        const row = document.createElement('tr');
        const time = new Date().toLocaleTimeString();
        const pid = Math.floor(Math.random() * 9000) + 1000;
        const load = (Math.random() * 100).toFixed(2) + "%";
        
        row.innerHTML = `<td>${time}</td><td>PID_${pid}</td><td>${load}</td><td style="color:#fff">EXECUTING</td>`;
        
        if (body.children.length > 12) body.removeChild(body.firstChild);
        body.appendChild(row);

        // Update Gauges
        cpuBar.style.width = Math.random() * 100 + "%";
        memBar.style.width = Math.random() * 100 + "%";
    }

    setInterval(addRow, 800);
</script>

</body>
</html>






new code 3d 

<!DOCTYPE html>
<html>
<head>
<style>
    body {
        margin: 0;
        height: 100vh;
        background: #000;
        overflow: hidden;
        display: flex;
        justify-content: center;
        align-items: center;
        /* THE MAGIC FILTER: Contrast + Blur creates the liquid effect */
        filter: contrast(30);
    }

    .container {
        position: relative;
        width: 100%;
        height: 100%;
        background: #000;
        filter: blur(15px); /* Liquid effect core */
    }

    .ball {
        position: absolute;
        width: 100px;
        height: 100px;
        background: #fff;
        border-radius: 50%;
        pointer-events: none;
    }

    .mouse-ball {
        position: absolute;
        width: 150px;
        height: 150px;
        background: #00ffaa; /* Neon Liquid */
        border-radius: 50%;
        transform: translate(-50%, -50%);
        transition: width 0.3s, height 0.3s;
    }

    #hud {
        position: absolute;
        top: 50%; left: 50%;
        transform: translate(-50%, -50%);
        filter: blur(0) contrast(1); /* Break out of the filter */
        color: #fff;
        font-family: 'Arial Black', sans-serif;
        font-size: 80px;
        z-index: 100;
        pointer-events: none;
        mix-blend-mode: difference;
    }
</style>
</head>
<body>

<div id="hud">-10</div>

<div class="container" id="container">
    <div class="mouse-ball" id="cursor"></div>
    </div>

<script>
    const container = document.getElementById('container');
    const cursor = document.getElementById('cursor');
    const balls = [];

    // Mouse Tracking
    document.addEventListener('mousemove', (e) => {
        cursor.style.left = e.clientX + 'px';
        cursor.style.top = e.clientY + 'px';
    });

    // Autonomous Liquid Particles
    class Ball {
        constructor() {
            this.el = document.createElement('div');
            this.el.className = 'ball';
            container.appendChild(this.el);
            this.x = Math.random() * window.innerWidth;
            this.y = Math.random() * window.innerHeight;
            this.vx = (Math.random() - 0.5) * 10;
            this.vy = (Math.random() - 0.5) * 10;
            this.size = Math.random() * 80 + 40;
            this.el.style.width = this.size + 'px';
            this.el.style.height = this.size + 'px';
        }

        update() {
            this.x += this.vx;
            this.y += this.vy;

            if (this.x < 0 || this.x > window.innerWidth) this.vx *= -1;
            if (this.y < 0 || this.y > window.innerHeight) this.vy *= -1;

            this.el.style.left = this.x + 'px';
            this.el.style.top = this.y + 'px';
        }
    }

    for (let i = 0; i < 15; i++) {
        balls.push(new Ball());
    }

    function animate() {
        balls.forEach(b => b.update());
        requestAnimationFrame(animate);
    }

    animate();
</script>

</body>
</html>


new code 3D 


<!DOCTYPE html>
<html>
<head>
<style>
    :root {
        --bg: #e0e5ec;
        --shadow-light: #ffffff;
        --shadow-dark: #a3b1c6;
        --accent: #6d5dfc;
    }

    body {
        background-color: var(--bg);
        margin: 0;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .glass-console {
        width: 400px;
        padding: 40px;
        border-radius: 50px;
        background: var(--bg);
        box-shadow: 20px 20px 60px var(--shadow-dark),
                   -20px -20px 60px var(--shadow-light);
        text-align: center;
    }

    h1 { color: #444; font-weight: 300; letter-spacing: 2px; margin-bottom: 30px; }

    /* The Interactive Grid */
    .grid-input {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 20px;
        margin-bottom: 30px;
    }

    .btn {
        width: 80px;
        height: 80px;
        border-radius: 20px;
        background: var(--bg);
        border: none;
        box-shadow: 6px 6px 12px var(--shadow-dark),
                   -6px -6px 12px var(--shadow-light);
        cursor: pointer;
        font-size: 18px;
        color: #666;
        transition: all 0.2s ease;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    /* Pressed Effect: The "Karwai" of Neumorphism */
    .btn:active {
        box-shadow: inset 6px 6px 12px var(--shadow-dark),
                    inset -6px -6px 12px var(--shadow-light);
        color: var(--accent);
        transform: scale(0.98);
    }

    #display {
        width: 100%;
        height: 60px;
        background: var(--bg);
        border-radius: 15px;
        box-shadow: inset 6px 6px 12px var(--shadow-dark),
                    inset -6px -6px 12px var(--shadow-light);
        margin-bottom: 30px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 24px;
        color: var(--accent);
        font-weight: bold;
    }
</style>
</head>
<body>

<div class="glass-console">
    <h1>WHITE SYSTEM</h1>
    <div id="display">0.00</div>
    
    <div class="grid-input">
        <button class="btn" onclick="update(10)">+10</button>
        <button class="btn" onclick="update(50)">+50</button>
        <button class="btn" onclick="update(100)">+100</button>
        <button class="btn" onclick="reset()" style="grid-column: span 3; width: 100%;">RESET CORE</button>
    </div>
    
    <p style="font-size: 10px; color: #999;">NEUMORPHIC_ARCHITECTURE_V.01</p>
</div>

<script>
    let currentVal = 0;
    const display = document.getElementById('display');

    function update(val) {
        currentVal += val;
        display.innerText = currentVal.toLocaleString();
        
        // Dynamic background logic
        const intensity = Math.min(currentVal / 10, 100);
        document.body.style.filter = `saturate(${1 + intensity/100})`;
    }

    function reset() {
        currentVal = 0;
        display.innerText = "0.00";
        document.body.style.filter = `saturate(1)`;
    }
</script>

</body>
</html>



new cod e  


<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        body { margin: 0; background: #f0f2f5; overflow: hidden; font-family: 'Inter', sans-serif; }
        canvas { position: absolute; inset: 0; z-index: 1; }

        /* Elite HUD */
        .hud-panel {
            position: absolute;
            top: 40px; left: 40px;
            padding: 25px;
            background: rgba(255, 255, 255, 0.7);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.5);
            border-radius: 24px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.05);
            z-index: 10;
            pointer-events: none;
            min-width: 200px;
        }

        .hud-title { font-size: 10px; font-weight: 800; color: #0071e3; letter-spacing: 2px; text-transform: uppercase; margin-bottom: 10px;}
        .hud-stat { font-size: 24px; font-weight: 300; color: #1d1d1f; }
        .hud-label { font-size: 11px; color: #86868b; margin-top: 5px; }
    </style>
</head>
<body>

<canvas id="canvas"></canvas>

<div class="hud-panel">
    <div class="hud-title">Kinetic System</div>
    <div class="hud-stat" id="speed-val">00.0</div>
    <div class="hud-label">MOUSE_VELOCITY_KMH</div>
    <hr style="border: 0; border-top: 1px solid #eee; margin: 15px 0;">
    <div class="hud-stat" id="obj-count">0</div>
    <div class="hud-label">ACTIVE_PHYSICS_NODES</div>
</div>

<script>
    const canvas = document.getElementById('canvas');
    const ctx = canvas.getContext('2d');
    const speedVal = document.getElementById('speed-val');
    const objCount = document.getElementById('obj-count');

    let particles = [];
    const mouse = { x: null, y: null, radius: 150 };

    window.addEventListener('mousemove', (e) => {
        mouse.x = e.x;
        mouse.y = e.y;
    });

    class Particle {
        constructor(x, y) {
            this.x = x;
            this.y = y;
            this.size = Math.random() * 5 + 2;
            this.baseX = this.x;
            this.baseY = this.y;
            this.density = (Math.random() * 40) + 5;
            this.color = Math.random() > 0.5 ? '#0071e3' : '#d1d1d6';
        }

        draw() {
            ctx.fillStyle = this.color;
            ctx.beginPath();
            if(this.size > 5) {
                ctx.rect(this.x, this.y, this.size, this.size);
            } else {
                ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
            }
            ctx.fill();
        }

        update() {
            let dx = mouse.x - this.x;
            let dy = mouse.y - this.y;
            let distance = Math.sqrt(dx * dx + dy * dy);
            let forceDirectionX = dx / distance;
            let forceDirectionY = dy / distance;
            let maxDistance = mouse.radius;
            let force = (maxDistance - distance) / maxDistance;
            let directionX = forceDirectionX * force * this.density;
            let directionY = forceDirectionY * force * this.density;

            if (distance < mouse.radius) {
                this.x -= directionX;
                this.y -= directionY;
            } else {
                if (this.x !== this.baseX) {
                    let dx = this.x - this.baseX;
                    this.x -= dx / 15;
                }
                if (this.y !== this.baseY) {
                    let dy = this.y - this.baseY;
                    this.y -= dy / 15;
                }
            }
        }
    }

    function init() {
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;
        particles = [];
        let numberOfParticles = (canvas.width * canvas.height) / 4000;
        objCount.innerText = Math.floor(numberOfParticles);
        
        for (let i = 0; i < numberOfParticles; i++) {
            let x = Math.random() * canvas.width;
            let y = Math.random() * canvas.height;
            particles.push(new Particle(x, y));
        }
    }

    let lastMouseX = 0;
    function animate() {
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        for (let i = 0; i < particles.length; i++) {
            particles[i].draw();
            particles[i].update();
        }
        
        // Velocity Tracking
        let currentVelocity = Math.abs(mouse.x - lastMouseX);
        speedVal.innerText = (currentVelocity).toFixed(1);
        lastMouseX = mouse.x;

        requestAnimationFrame(animate);
    }

    init();
    animate();
    window.addEventListener('resize', init);
</script>

</body>
</html>